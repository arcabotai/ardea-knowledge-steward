# Build report

Generated by Cad from Arca on 2026-06-17.

## Result

- Local workdir: `/root/ardea-knowledge-steward`
- GitHub: https://github.com/arcabotai/ardea-knowledge-steward
- Production: https://ardea-knowledge-steward.vercel.app
- Commit: see current `main`; final deployment summary records the exact SHA.

## Framework probe

- Eve CLI: `0.11.4`
- Eve status: beta / preview; APIs and behavior may change before GA.
- Node required by Eve: `24.x`; local system Node is `22.22.2`, so commands were run through `npx -p node@24 -p npm@latest ...`.
- `eve init --channel-web-nextjs ardea-knowledge-steward` created the project. It exited non-zero only after trying to launch the interactive TUI with `--input`, which is not usable in this non-interactive shell. The scaffold itself was created and used.

## Checks run

```text
npx -y -p node@24 -p npm@latest npm test
# pass: 3/3 node:test checks

npx -y -p node@24 -p npm@latest npm run typecheck
# pass

npx -y -p node@24 -p npm@latest npm run build
# pass: Next.js 16.2.6 production build

curl http://127.0.0.1:3127/
curl -X POST http://127.0.0.1:3127/api/ask
curl http://127.0.0.1:3127/api/node-status
curl http://127.0.0.1:3127/api/snap-market
# pass: local page and API routes return expected content/JSON

curl -I https://ardea-knowledge-steward.vercel.app
curl -X POST https://ardea-knowledge-steward.vercel.app/api/ask
curl https://ardea-knowledge-steward.vercel.app/api/node-status
curl https://ardea-knowledge-steward.vercel.app/api/snap-market
# pass: live deployment returns HTTP 200 and expected JSON
```

## Live API proof

- `/api/ask` returns grounded answers with confidence, labels, safety notes, and cited knowledge snippets.
- `/api/node-status` reached both `https://ardea.arcabot.ai` and `http://209.97.147.208:3381/v1/info` during verification. It explicitly warns that reachability is not full sync health.
- `/api/snap-market` fetched Dexscreener and returned corrected FDV using the user-confirmed `200_000_000_000` total supply.

## Browser proof

Browser navigation to `https://ardea-knowledge-steward.vercel.app` showed title:

```text
Ask Ardea — Hypersnap Knowledge Steward
```

The page rendered:

```text
Public answers for Hypersnap, Snapchain, and Farcaster-fork builders.
```

Clicking `Ask` in the browser produced a visible grounded answer and source cards.

## Caveats / next work

- The public web/API fallback is deterministic and does not require a model key. The Eve agent files and TypeScript tools are present, but production model calls require AI Gateway/provider credentials.
- Farcaster/Hypersnap mention handling is scaffolded in `integrations/farcaster-channel.example.ts`; live posting needs signer/API credentials and a final choice of native Snapchain vs temporary compatibility bridge.
- Vercel created the project under `arcas-projects-d844d101` and connected the GitHub repository.
- `npm install` reported 4 moderate dependency vulnerabilities from the fresh scaffold dependency tree; no forced audit fix was applied because that could introduce breaking changes in beta dependencies.
